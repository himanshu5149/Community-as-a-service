{
  "name": "caas-functions",
  "version": "1.0.0",
  "description": "CaaS Firebase Cloud Functions — replaces Railway/Node.js server",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "@google/genai": "^1.50.1",
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.0.0"
  }
}
